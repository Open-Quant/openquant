---
import Layout from "../layouts/Layout.astro";
---
<Layout title="Module Guide • OpenQuant-rs" description="Module-level guide to OpenQuant-rs for quant workflows.">
  <section class="hero">
    <h1>Subjects (AFML-Aligned)</h1>
    <p class="muted">This is the primary subject map. The same five dividers are reused in API and examples pages.</p>
  </section>

  <h2>1. Event-Driven Data and Labeling</h2>
  <p class="muted">AFML orientation: Ch. 2-3</p>
  <p><code>data_structures</code>, <code>filters</code>, <code>labeling</code>, <code>sample_weights</code></p>

  <h2>2. Sampling, Validation and ML Diagnostics</h2>
  <p class="muted">AFML orientation: Ch. 4, 7</p>
  <p><code>sampling</code>, <code>cross_validation</code>, <code>feature_importance</code>, <code>fingerprint</code>, <code>sb_bagging</code>, <code>ef3m</code></p>

  <h2>3. Position Sizing and Trade Construction</h2>
  <p class="muted">AFML orientation: Ch. 10 + execution tooling</p>
  <p><code>bet_sizing</code>, <code>etf_trick</code></p>

  <h2>4. Portfolio Construction and Risk</h2>
  <p class="muted">AFML orientation: Ch. 16 + risk overlays</p>
  <p><code>portfolio_optimization</code>, <code>cla</code>, <code>hrp</code>, <code>hcaa</code>, <code>onc</code>, <code>risk_metrics</code>, <code>backtest_statistics</code></p>

  <h2>5. Market Microstructure, Dependence and Regime Detection</h2>
  <p class="muted">AFML orientation: microstructure and structural diagnostics</p>
  <p><code>microstructural_features</code>, <code>codependence</code>, <code>structural_breaks</code>, <code>fracdiff</code>, <code>util::fast_ewma</code>, <code>util::volatility</code></p>

  <h2>Usage Rule</h2>
  <p>Start in this subject map, then use <code>API by Subject</code> and <code>Examples by Subject</code> with the same divider names.</p>
</Layout>
